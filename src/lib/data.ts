
// This data is now managed in Firestore and fetched in the settings page.
// The default categories are now seeded in `src/app/(app)/settings/page.tsx`
// if no categories exist for the user.
export const categories = [];

// Tasks are stored in Firestore, so this is empty.
export const tasks = [];
